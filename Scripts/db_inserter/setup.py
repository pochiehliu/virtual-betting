from setuptools import setup

setup(name='db_inserter',
      version='0.1',
      description='Tools for inserting new data into database',
      author='Peter Grantcharov',
      author_email='peter.grantcharov@gmail.com',
      packages=['db_inserter'],
      zip_safe=False)
